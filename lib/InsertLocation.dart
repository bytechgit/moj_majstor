import 'package:flutter/cupertino.dart';

class InsertLocation extends StatelessWidget {
  const InsertLocation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
